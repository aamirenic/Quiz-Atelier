# ─── secrets / signing ───────────────────────────
android/app/release.keystore
*.keystore
*.jks

# ─── live server data (user data, never in git) ──
server/data/db.json
server/data/backups/
server/data/*.backup*.json
server/data/*.bak-*.json
server/data/*.bak*
server/*.log

# ─── build artifacts ─────────────────────────────
*.apk
*.idsig
android/app/build/
android/.gradle/

# ─── toolchains & deps ───────────────────────────
node_modules/
android-build-tools/

# ─── personal files, not project code ────────────
demo data/
.freebuff/

# ─── personal demo dataset & one-off tooling ─────
content.json
**/content.json
make_icons.py

# ─── OS / editor noise ───────────────────────────
Thumbs.db
.DS_Store
desktop.ini
