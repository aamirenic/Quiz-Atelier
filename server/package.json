{
  "name": "quiz-atelier-server",
  "version": "1.0.0",
  "private": true,
  "description": "Quiz Atelier — auth + cloud persistence backend (no external dependencies).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
